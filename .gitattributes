# 기본: 텍스트 파일은 커밋 시 LF로 정규화, 체크아웃 시 OS 기본값 사용
* text=auto eol=lf

# 셸 스크립트는 항상 LF (Windows에서 CRLF로 바뀌면 실행 불가)
gradlew text eol=lf

# Windows 배치 파일은 항상 CRLF
*.bat text eol=crlf
gradlew.bat text eol=crlf

# 바이너리는 정규화하지 않음
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
